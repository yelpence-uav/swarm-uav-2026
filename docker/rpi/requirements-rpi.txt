# YELPENÇE SÜRÜ İHA - RPi 4 GÖREV BİLGİSAYARI
# Görev bilgisayarında gereken bağımlılıklar burada takip edilir.
# Listelenen tüm paketlerin sürümleri "[PAKET]==[SÜRÜM]" şeklinde belirtilmelidir. Aksi belirtilmediği sürece en son sürümler tercih edilmelidir.
# Bir bağımlılığın spesifik bir sürümü gerekiyorsa yorum satırı ekleyerek belirtiniz.
# Tüm python paketlerini https://pypi.org/ adresinde bulabilirsiniz.

pymavlink==2.4.49
pyserial==3.5
pyros-genmsg==0.5.8
opencv-python-headless==4.10.0.84  # numpy <2.0 uyumluluğu için bu sürüm gerekiyor. Belleği verimli kullanması için headless tercih edilmelidir.
numpy<2.0.0                        # ROS2 uyumluluğu için <2.0 sürümleri gerekiyor.
pyzbar==0.1.9
qrcode==8.2
kconfiglib==14.1.0
jinja2==3.1.6
jsonschema==4.26.0
packaging==26.0
toml==0.10.2
future==1.0.0
typeguard==4.5.1
pyyaml==6.0.3
catkin_pkg==1.1.0
lark==1.3.1
flask==3.1.3
flask-socketio==5.6.1
