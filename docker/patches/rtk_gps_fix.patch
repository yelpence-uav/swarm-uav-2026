diff --git a/CMakeLists.txt b/CMakeLists.txt
index fc57b95efc..f5ae4dff4b 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -119,6 +119,10 @@ execute_process(
 	WORKING_DIRECTORY ${PX4_SOURCE_DIR}
 	)
 
+if(NOT PX4_GIT_TAG)
+	set(PX4_GIT_TAG "v1.15.0-0-gabcdef")
+endif()
+
 # git describe to X.Y.Z version
 string(REPLACE "." ";" VERSION_LIST ${PX4_GIT_TAG})
 
diff --git a/ROMFS/px4fmu_common/init.d-posix/airframes/4001_gz_x500 b/ROMFS/px4fmu_common/init.d-posix/airframes/4001_gz_x500
index bb6bedd386..f6c87e3522 100644
--- a/ROMFS/px4fmu_common/init.d-posix/airframes/4001_gz_x500
+++ b/ROMFS/px4fmu_common/init.d-posix/airframes/4001_gz_x500
@@ -14,9 +14,16 @@ PX4_SIM_MODEL=${PX4_SIM_MODEL:=x500}
 param set-default SIM_GZ_EN 1
 
 param set-default SENS_EN_GPSSIM 1
-param set-default SENS_EN_BAROSIM 0
+param set-default SENS_EN_BAROSIM 1
 param set-default SENS_EN_MAGSIM 1
 
+param set-default UXRCE_DDS_SYNCT 0
+
+# EKF2 Irtifa ve RTK Ayarlari
+param set-default EKF2_HGT_MODE 1
+param set-default EKF2_GPS_CHECK 0
+
 param set-default CA_AIRFRAME 0
 param set-default CA_ROTOR_COUNT 4
 
@@ -51,4 +58,6 @@ param set-default SIM_GZ_EC_MAX2 1000
 param set-default SIM_GZ_EC_MAX3 1000
 param set-default SIM_GZ_EC_MAX4 1000
 
-param set-default MPC_THR_HOVER 0.60
+param set-default MPC_THR_HOVER 0.55
+param set-default MPC_Z_P 1.0
+
diff --git a/src/lib/version/CMakeLists.txt b/src/lib/version/CMakeLists.txt
index 6b91e9cb11..51b74004ab 100644
--- a/src/lib/version/CMakeLists.txt
+++ b/src/lib/version/CMakeLists.txt
@@ -44,11 +44,13 @@ if (EXISTS ${PX4_SOURCE_DIR}/.git)
 		get_filename_component(git_dir_path "${git_dir_path}" REALPATH BASE_DIR ${PX4_SOURCE_DIR})
 	endif()
 else()
-	message(FATAL_ERROR "is not a git repository")
+	# message(FATAL_ERROR "is not a git repository")
+	set(git_dir_path ${PX4_SOURCE_DIR})
 endif()
 
 if(NOT IS_DIRECTORY "${git_dir_path}")
-	message(FATAL_ERROR "${git_dir_path} is not a directory")
+	# message(FATAL_ERROR "${git_dir_path} is not a directory")
+	set(git_dir_path ${PX4_SOURCE_DIR})
 endif()
 
 set(px4_git_ver_header ${CMAKE_CURRENT_BINARY_DIR}/build_git_version.h)
@@ -56,8 +58,6 @@ add_custom_command(OUTPUT ${px4_git_ver_header}
 	COMMAND ${PYTHON_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/px_update_git_header.py ${px4_git_ver_header} --validate
 	DEPENDS
 		${CMAKE_CURRENT_SOURCE_DIR}/px_update_git_header.py
-		${git_dir_path}/HEAD
-		${git_dir_path}/index
 	WORKING_DIRECTORY ${PX4_SOURCE_DIR}
 	COMMENT "Generating git version header"
 	)
diff --git a/src/lib/version/px_update_git_header.py b/src/lib/version/px_update_git_header.py
index 3a4fdff98e..d1f52f841e 100755
--- a/src/lib/version/px_update_git_header.py
+++ b/src/lib/version/px_update_git_header.py
@@ -36,16 +36,19 @@ header = """
 
 
 # PX4
-git_describe_cmd = 'git describe --exclude ext/* --always --tags --dirty'
-git_tag = subprocess.check_output(git_describe_cmd.split(),
-                                  stderr=subprocess.STDOUT).decode('utf-8').strip()
+try:
+    git_describe_cmd = 'git describe --exclude ext/* --always --tags --dirty'
+    git_tag = subprocess.check_output(git_describe_cmd.split(),
+                                      stderr=subprocess.STDOUT).decode('utf-8').strip()
+except:
+    git_tag = "v1.15.0-0-gabcdef"
 
 try:
     # get the tag if we're on a tagged commit
     tag_or_branch = subprocess.check_output((git_describe_cmd+' --exact-match').split(),
                                             stderr=subprocess.STDOUT).decode('utf-8').strip()
 except:
-    tag_or_branch = None
+    tag_or_branch = "v1.15.0"
 
 if validate:
     if verbose:
@@ -83,13 +86,18 @@ if validate:
         print("")
         sys.exit(1)
 
-git_version = subprocess.check_output('git rev-parse --verify HEAD'.split(),
-                                      stderr=subprocess.STDOUT).decode('utf-8').strip()
+try:
+    git_version = subprocess.check_output('git rev-parse --verify HEAD'.split(),
+                                          stderr=subprocess.STDOUT).decode('utf-8').strip()
+except:
+    git_version = "abcdef0123456789012345678901234567890123"
+
 try:
     git_branch_name = subprocess.check_output('git symbolic-ref -q --short HEAD'.split(),
                                           stderr=subprocess.STDOUT).decode('utf-8').strip()
 except:
-    git_branch_name = ''
+    git_branch_name = 'master'
+
 git_version_short = git_version[0:16]
 
 # OEM version
@@ -121,8 +129,12 @@ header += f"""
 
 # Mavlink
 if (os.path.exists('src/modules/mavlink/mavlink/.git')):
-    mavlink_git_version = subprocess.check_output('git rev-parse --verify HEAD'.split(),
-                                      cwd='src/modules/mavlink/mavlink', stderr=subprocess.STDOUT).decode('utf-8').strip()
+    try:
+        mavlink_git_version = subprocess.check_output('git rev-parse --verify HEAD'.split(),
+                                          cwd='src/modules/mavlink/mavlink', stderr=subprocess.STDOUT).decode('utf-8').strip()
+    except:
+        mavlink_git_version = "abcdef0123456789"
+
     mavlink_git_version_short = mavlink_git_version[0:16]
 
     header += f"""
@@ -133,12 +145,17 @@ if (os.path.exists('src/modules/mavlink/mavlink/.git')):
 
 # NuttX
 if (os.path.exists('platforms/nuttx/NuttX/nuttx/.git')):
-    nuttx_git_tags = subprocess.check_output('git -c versionsort.suffix=- tag --sort=v:refname'.split(),
-                                  cwd='platforms/nuttx/NuttX/nuttx', stderr=subprocess.STDOUT).decode('utf-8').strip()
-    nuttx_git_tag = re.findall(r'nuttx-[0-9]+\.[0-9]+\.[0-9]+', nuttx_git_tags)[-1].replace("nuttx-", "v")
-    nuttx_git_tag = re.sub('-.*', '.0', nuttx_git_tag)
-    nuttx_git_version = subprocess.check_output('git rev-parse --verify HEAD'.split(),
+    try:
+        nuttx_git_tags = subprocess.check_output('git -c versionsort.suffix=- tag --sort=v:refname'.split(),
                                       cwd='platforms/nuttx/NuttX/nuttx', stderr=subprocess.STDOUT).decode('utf-8').strip()
+        nuttx_git_tag = re.findall(r'nuttx-[0-9]+\.[0-9]+\.[0-9]+', nuttx_git_tags)[-1].replace("nuttx-", "v")
+        nuttx_git_tag = re.sub('-.*', '.0', nuttx_git_tag)
+        nuttx_git_version = subprocess.check_output('git rev-parse --verify HEAD'.split(),
+                                          cwd='platforms/nuttx/NuttX/nuttx', stderr=subprocess.STDOUT).decode('utf-8').strip()
+    except:
+        nuttx_git_tag = "v1.15.0"
+        nuttx_git_version = "abcdef0123456789"
+
     nuttx_git_version_short = nuttx_git_version[0:16]
 
     header += f"""
diff --git a/src/modules/simulation/sensor_gps_sim/SensorGpsSim.cpp b/src/modules/simulation/sensor_gps_sim/SensorGpsSim.cpp
index 5641706345..9a0430c40e 100644
--- a/src/modules/simulation/sensor_gps_sim/SensorGpsSim.cpp
+++ b/src/modules/simulation/sensor_gps_sim/SensorGpsSim.cpp
@@ -114,11 +114,10 @@ void SensorGpsSim::Run()
 		vehicle_global_position_s gpos{};
 		_vehicle_global_position_sub.copy(&gpos);
 
-		double latitude = gpos.lat + math::degrees((double)generate_wgn() * 0.2 / CONSTANTS_RADIUS_OF_EARTH);
-		double longitude = gpos.lon + math::degrees((double)generate_wgn() * 0.2 / CONSTANTS_RADIUS_OF_EARTH);
-		double altitude = (double)(gpos.alt + (generate_wgn() * 0.5f));
-
-		Vector3f gps_vel = Vector3f{lpos.vx, lpos.vy, lpos.vz} + noiseGauss3f(0.06f, 0.077f, 0.158f);
+		double latitude = gpos.lat;
+		double longitude = gpos.lon;
+		double altitude = gpos.alt;
+		Vector3f gps_vel = Vector3f{lpos.vx, lpos.vy, lpos.vz};
 
 		// device id
 		device::Device::DeviceId device_id;
@@ -131,13 +130,13 @@ void SensorGpsSim::Run()
 
 		if (_sim_gps_used.get() >= 4) {
 			// fix
-			sensor_gps.fix_type = 3; // 3D fix
-			sensor_gps.s_variance_m_s = 0.4f;
-			sensor_gps.c_variance_rad = 0.1f;
-			sensor_gps.eph = 0.9f;
-			sensor_gps.epv = 1.78f;
-			sensor_gps.hdop = 0.7f;
-			sensor_gps.vdop = 1.1f;
+			sensor_gps.fix_type = 6; // RTK fix
+			sensor_gps.s_variance_m_s = 0.1f;
+			sensor_gps.c_variance_rad = 0.01f;
+			sensor_gps.eph = 0.05f;
+			sensor_gps.epv = 0.05f;
+			sensor_gps.hdop = 0.1f;
+			sensor_gps.vdop = 0.1f;
 
 		} else {
 			// no fix
diff --git a/src/modules/simulation/sensor_gps_sim/SensorGpsSim.hpp b/src/modules/simulation/sensor_gps_sim/SensorGpsSim.hpp
index fac4e5201e..454e741341 100644
--- a/src/modules/simulation/sensor_gps_sim/SensorGpsSim.hpp
+++ b/src/modules/simulation/sensor_gps_sim/SensorGpsSim.hpp
@@ -85,4 +85,9 @@ private:
 	DEFINE_PARAMETERS(
 		(ParamInt<px4::params::SIM_GPS_USED>) _sim_gps_used
 	)
+
+	double _last_lat{0.0};
+	double _last_lon{0.0};
+	double _last_alt{0.0};
+	bool _first_run{true};
 };
diff --git a/src/modules/uxrce_dds_client/dds_topics.yaml b/src/modules/uxrce_dds_client/dds_topics.yaml
index 255c8e0b54..fcc9461b60 100644
--- a/src/modules/uxrce_dds_client/dds_topics.yaml
+++ b/src/modules/uxrce_dds_client/dds_topics.yaml
@@ -160,5 +160,8 @@ subscriptions:
   - topic: /fmu/in/aux_global_position
     type: px4_msgs::msg::VehicleGlobalPosition
 
+  - topic: /fmu/in/gps_inject_data
+    type: px4_msgs::msg::GpsInjectData
+
 # Create uORB::PublicationMulti
 subscriptions_multi:
